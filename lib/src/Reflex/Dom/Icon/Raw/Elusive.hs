{-# LANGUAGE OverloadedStrings #-}

{- |
Module      : Reflex.Dom.Icon.Raw.Elusive
Description : Auto-generated file to give hs-constants for css-file.
Copyright   : (c) gspia 2017- 
Licence     : BSD3
Maintainer  : gspia

= Elusive

This file contains hs-constants for a css-file. The generated file is included
in the reflex-dom-themes -package, see https://github.com/gspia/reflex-dom-themes
and the examples in there.

To use the results with reflex-dom-themes, the app should include the chosen
css-files (or links to them). Some of the css-files can be found from various CDNs
together with the fonts. If not, then you should get the fonts and put them into
correct sub-directory.

This file was generated with https://github.com/gspia/css2HsClassName

-}

module Reflex.Dom.Icon.Raw.Elusive where

import Reflex.Dom.HTML5.Attrs (ClassName (ClassName))


-- This file is automatically generated. 


-- | A ClassName for el
el :: ClassName
el = ClassName "el"

-- | A ClassName for el-2x
el2X :: ClassName
el2X = ClassName "el-2x"

-- | A ClassName for el-3x
el3X :: ClassName
el3X = ClassName "el-3x"

-- | A ClassName for el-4x
el4X :: ClassName
el4X = ClassName "el-4x"

-- | A ClassName for el-5x
el5X :: ClassName
el5X = ClassName "el-5x"

-- | A ClassName for el-address-book
elAddressBook :: ClassName
elAddressBook = ClassName "el-address-book"

-- | A ClassName for el-address-book-alt
elAddressBookAlt :: ClassName
elAddressBookAlt = ClassName "el-address-book-alt"

-- | A ClassName for el-adjust
elAdjust :: ClassName
elAdjust = ClassName "el-adjust"

-- | A ClassName for el-adjust-alt
elAdjustAlt :: ClassName
elAdjustAlt = ClassName "el-adjust-alt"

-- | A ClassName for el-adult
elAdult :: ClassName
elAdult = ClassName "el-adult"

-- | A ClassName for el-align-center
elAlignCenter :: ClassName
elAlignCenter = ClassName "el-align-center"

-- | A ClassName for el-align-justify
elAlignJustify :: ClassName
elAlignJustify = ClassName "el-align-justify"

-- | A ClassName for el-align-left
elAlignLeft :: ClassName
elAlignLeft = ClassName "el-align-left"

-- | A ClassName for el-align-right
elAlignRight :: ClassName
elAlignRight = ClassName "el-align-right"

-- | A ClassName for el-arrow-down
elArrowDown :: ClassName
elArrowDown = ClassName "el-arrow-down"

-- | A ClassName for el-arrow-left
elArrowLeft :: ClassName
elArrowLeft = ClassName "el-arrow-left"

-- | A ClassName for el-arrow-right
elArrowRight :: ClassName
elArrowRight = ClassName "el-arrow-right"

-- | A ClassName for el-arrow-up
elArrowUp :: ClassName
elArrowUp = ClassName "el-arrow-up"

-- | A ClassName for el-asl
elAsl :: ClassName
elAsl = ClassName "el-asl"

-- | A ClassName for el-asterisk
elAsterisk :: ClassName
elAsterisk = ClassName "el-asterisk"

-- | A ClassName for el-backward
elBackward :: ClassName
elBackward = ClassName "el-backward"

-- | A ClassName for el-ban-circle
elBanCircle :: ClassName
elBanCircle = ClassName "el-ban-circle"

-- | A ClassName for el-barcode
elBarcode :: ClassName
elBarcode = ClassName "el-barcode"

-- | A ClassName for el-behance
elBehance :: ClassName
elBehance = ClassName "el-behance"

-- | A ClassName for el-bell
elBell :: ClassName
elBell = ClassName "el-bell"

-- | A ClassName for el-blind
elBlind :: ClassName
elBlind = ClassName "el-blind"

-- | A ClassName for el-blogger
elBlogger :: ClassName
elBlogger = ClassName "el-blogger"

-- | A ClassName for el-bold
elBold :: ClassName
elBold = ClassName "el-bold"

-- | A ClassName for el-book
elBook :: ClassName
elBook = ClassName "el-book"

-- | A ClassName for el-bookmark
elBookmark :: ClassName
elBookmark = ClassName "el-bookmark"

-- | A ClassName for el-bookmark-empty
elBookmarkEmpty :: ClassName
elBookmarkEmpty = ClassName "el-bookmark-empty"

-- | A ClassName for el-border
elBorder :: ClassName
elBorder = ClassName "el-border"

-- | A ClassName for el-braille
elBraille :: ClassName
elBraille = ClassName "el-braille"

-- | A ClassName for el-briefcase
elBriefcase :: ClassName
elBriefcase = ClassName "el-briefcase"

-- | A ClassName for el-broom
elBroom :: ClassName
elBroom = ClassName "el-broom"

-- | A ClassName for el-brush
elBrush :: ClassName
elBrush = ClassName "el-brush"

-- | A ClassName for el-bulb
elBulb :: ClassName
elBulb = ClassName "el-bulb"

-- | A ClassName for el-bullhorn
elBullhorn :: ClassName
elBullhorn = ClassName "el-bullhorn"

-- | A ClassName for el-calendar
elCalendar :: ClassName
elCalendar = ClassName "el-calendar"

-- | A ClassName for el-calendar-sign
elCalendarSign :: ClassName
elCalendarSign = ClassName "el-calendar-sign"

-- | A ClassName for el-camera
elCamera :: ClassName
elCamera = ClassName "el-camera"

-- | A ClassName for el-car
elCar :: ClassName
elCar = ClassName "el-car"

-- | A ClassName for el-caret-down
elCaretDown :: ClassName
elCaretDown = ClassName "el-caret-down"

-- | A ClassName for el-caret-left
elCaretLeft :: ClassName
elCaretLeft = ClassName "el-caret-left"

-- | A ClassName for el-caret-right
elCaretRight :: ClassName
elCaretRight = ClassName "el-caret-right"

-- | A ClassName for el-caret-up
elCaretUp :: ClassName
elCaretUp = ClassName "el-caret-up"

-- | A ClassName for el-cc
elCc :: ClassName
elCc = ClassName "el-cc"

-- | A ClassName for el-certificate
elCertificate :: ClassName
elCertificate = ClassName "el-certificate"

-- | A ClassName for el-check
elCheck :: ClassName
elCheck = ClassName "el-check"

-- | A ClassName for el-check-empty
elCheckEmpty :: ClassName
elCheckEmpty = ClassName "el-check-empty"

-- | A ClassName for el-chevron-down
elChevronDown :: ClassName
elChevronDown = ClassName "el-chevron-down"

-- | A ClassName for el-chevron-left
elChevronLeft :: ClassName
elChevronLeft = ClassName "el-chevron-left"

-- | A ClassName for el-chevron-right
elChevronRight :: ClassName
elChevronRight = ClassName "el-chevron-right"

-- | A ClassName for el-chevron-up
elChevronUp :: ClassName
elChevronUp = ClassName "el-chevron-up"

-- | A ClassName for el-child
elChild :: ClassName
elChild = ClassName "el-child"

-- | A ClassName for el-circle-arrow-down
elCircleArrowDown :: ClassName
elCircleArrowDown = ClassName "el-circle-arrow-down"

-- | A ClassName for el-circle-arrow-left
elCircleArrowLeft :: ClassName
elCircleArrowLeft = ClassName "el-circle-arrow-left"

-- | A ClassName for el-circle-arrow-right
elCircleArrowRight :: ClassName
elCircleArrowRight = ClassName "el-circle-arrow-right"

-- | A ClassName for el-circle-arrow-up
elCircleArrowUp :: ClassName
elCircleArrowUp = ClassName "el-circle-arrow-up"

-- | A ClassName for el-cloud
elCloud :: ClassName
elCloud = ClassName "el-cloud"

-- | A ClassName for el-cloud-alt
elCloudAlt :: ClassName
elCloudAlt = ClassName "el-cloud-alt"

-- | A ClassName for el-cog
elCog :: ClassName
elCog = ClassName "el-cog"

-- | A ClassName for el-cog-alt
elCogAlt :: ClassName
elCogAlt = ClassName "el-cog-alt"

-- | A ClassName for el-cogs
elCogs :: ClassName
elCogs = ClassName "el-cogs"

-- | A ClassName for el-comment
elComment :: ClassName
elComment = ClassName "el-comment"

-- | A ClassName for el-comment-alt
elCommentAlt :: ClassName
elCommentAlt = ClassName "el-comment-alt"

-- | A ClassName for el-compass
elCompass :: ClassName
elCompass = ClassName "el-compass"

-- | A ClassName for el-compass-alt
elCompassAlt :: ClassName
elCompassAlt = ClassName "el-compass-alt"

-- | A ClassName for el-credit-card
elCreditCard :: ClassName
elCreditCard = ClassName "el-credit-card"

-- | A ClassName for el-css
elCss :: ClassName
elCss = ClassName "el-css"

-- | A ClassName for el-dashboard
elDashboard :: ClassName
elDashboard = ClassName "el-dashboard"

-- | A ClassName for el-delicious
elDelicious :: ClassName
elDelicious = ClassName "el-delicious"

-- | A ClassName for el-deviantart
elDeviantart :: ClassName
elDeviantart = ClassName "el-deviantart"

-- | A ClassName for el-digg
elDigg :: ClassName
elDigg = ClassName "el-digg"

-- | A ClassName for el-download
elDownload :: ClassName
elDownload = ClassName "el-download"

-- | A ClassName for el-download-alt
elDownloadAlt :: ClassName
elDownloadAlt = ClassName "el-download-alt"

-- | A ClassName for el-dribbble
elDribbble :: ClassName
elDribbble = ClassName "el-dribbble"

-- | A ClassName for el-edit
elEdit :: ClassName
elEdit = ClassName "el-edit"

-- | A ClassName for el-eject
elEject :: ClassName
elEject = ClassName "el-eject"

-- | A ClassName for el-envelope
elEnvelope :: ClassName
elEnvelope = ClassName "el-envelope"

-- | A ClassName for el-envelope-alt
elEnvelopeAlt :: ClassName
elEnvelopeAlt = ClassName "el-envelope-alt"

-- | A ClassName for el-error
elError :: ClassName
elError = ClassName "el-error"

-- | A ClassName for el-error-alt
elErrorAlt :: ClassName
elErrorAlt = ClassName "el-error-alt"

-- | A ClassName for el-eur
elEur :: ClassName
elEur = ClassName "el-eur"

-- | A ClassName for el-exclamation-sign
elExclamationSign :: ClassName
elExclamationSign = ClassName "el-exclamation-sign"

-- | A ClassName for el-eye-close
elEyeClose :: ClassName
elEyeClose = ClassName "el-eye-close"

-- | A ClassName for el-eye-open
elEyeOpen :: ClassName
elEyeOpen = ClassName "el-eye-open"

-- | A ClassName for el-facebook
elFacebook :: ClassName
elFacebook = ClassName "el-facebook"

-- | A ClassName for el-facetime-video
elFacetimeVideo :: ClassName
elFacetimeVideo = ClassName "el-facetime-video"

-- | A ClassName for el-fast-backward
elFastBackward :: ClassName
elFastBackward = ClassName "el-fast-backward"

-- | A ClassName for el-fast-forward
elFastForward :: ClassName
elFastForward = ClassName "el-fast-forward"

-- | A ClassName for el-female
elFemale :: ClassName
elFemale = ClassName "el-female"

-- | A ClassName for el-file
elFile :: ClassName
elFile = ClassName "el-file"

-- | A ClassName for el-file-alt
elFileAlt :: ClassName
elFileAlt = ClassName "el-file-alt"

-- | A ClassName for el-file-edit
elFileEdit :: ClassName
elFileEdit = ClassName "el-file-edit"

-- | A ClassName for el-file-edit-alt
elFileEditAlt :: ClassName
elFileEditAlt = ClassName "el-file-edit-alt"

-- | A ClassName for el-file-new
elFileNew :: ClassName
elFileNew = ClassName "el-file-new"

-- | A ClassName for el-file-new-alt
elFileNewAlt :: ClassName
elFileNewAlt = ClassName "el-file-new-alt"

-- | A ClassName for el-film
elFilm :: ClassName
elFilm = ClassName "el-film"

-- | A ClassName for el-filter
elFilter :: ClassName
elFilter = ClassName "el-filter"

-- | A ClassName for el-fire
elFire :: ClassName
elFire = ClassName "el-fire"

-- | A ClassName for el-flag
elFlag :: ClassName
elFlag = ClassName "el-flag"

-- | A ClassName for el-flag-alt
elFlagAlt :: ClassName
elFlagAlt = ClassName "el-flag-alt"

-- | A ClassName for el-flickr
elFlickr :: ClassName
elFlickr = ClassName "el-flickr"

-- | A ClassName for el-flip-horizontal
elFlipHorizontal :: ClassName
elFlipHorizontal = ClassName "el-flip-horizontal"

-- | A ClassName for el-flip-vertical
elFlipVertical :: ClassName
elFlipVertical = ClassName "el-flip-vertical"

-- | A ClassName for el-folder
elFolder :: ClassName
elFolder = ClassName "el-folder"

-- | A ClassName for el-folder-close
elFolderClose :: ClassName
elFolderClose = ClassName "el-folder-close"

-- | A ClassName for el-folder-open
elFolderOpen :: ClassName
elFolderOpen = ClassName "el-folder-open"

-- | A ClassName for el-folder-sign
elFolderSign :: ClassName
elFolderSign = ClassName "el-folder-sign"

-- | A ClassName for el-font
elFont :: ClassName
elFont = ClassName "el-font"

-- | A ClassName for el-fontsize
elFontsize :: ClassName
elFontsize = ClassName "el-fontsize"

-- | A ClassName for el-fork
elFork :: ClassName
elFork = ClassName "el-fork"

-- | A ClassName for el-forward
elForward :: ClassName
elForward = ClassName "el-forward"

-- | A ClassName for el-forward-alt
elForwardAlt :: ClassName
elForwardAlt = ClassName "el-forward-alt"

-- | A ClassName for el-foursquare
elFoursquare :: ClassName
elFoursquare = ClassName "el-foursquare"

-- | A ClassName for el-friendfeed
elFriendfeed :: ClassName
elFriendfeed = ClassName "el-friendfeed"

-- | A ClassName for el-friendfeed-rect
elFriendfeedRect :: ClassName
elFriendfeedRect = ClassName "el-friendfeed-rect"

-- | A ClassName for el-fullscreen
elFullscreen :: ClassName
elFullscreen = ClassName "el-fullscreen"

-- | A ClassName for el-fw
elFw :: ClassName
elFw = ClassName "el-fw"

-- | A ClassName for el-gbp
elGbp :: ClassName
elGbp = ClassName "el-gbp"

-- | A ClassName for el-gift
elGift :: ClassName
elGift = ClassName "el-gift"

-- | A ClassName for el-github
elGithub :: ClassName
elGithub = ClassName "el-github"

-- | A ClassName for el-github-text
elGithubText :: ClassName
elGithubText = ClassName "el-github-text"

-- | A ClassName for el-glass
elGlass :: ClassName
elGlass = ClassName "el-glass"

-- | A ClassName for el-glasses
elGlasses :: ClassName
elGlasses = ClassName "el-glasses"

-- | A ClassName for el-globe
elGlobe :: ClassName
elGlobe = ClassName "el-globe"

-- | A ClassName for el-globe-alt
elGlobeAlt :: ClassName
elGlobeAlt = ClassName "el-globe-alt"

-- | A ClassName for el-googleplus
elGoogleplus :: ClassName
elGoogleplus = ClassName "el-googleplus"

-- | A ClassName for el-graph
elGraph :: ClassName
elGraph = ClassName "el-graph"

-- | A ClassName for el-graph-alt
elGraphAlt :: ClassName
elGraphAlt = ClassName "el-graph-alt"

-- | A ClassName for el-group
elGroup :: ClassName
elGroup = ClassName "el-group"

-- | A ClassName for el-group-alt
elGroupAlt :: ClassName
elGroupAlt = ClassName "el-group-alt"

-- | A ClassName for el-guidedog
elGuidedog :: ClassName
elGuidedog = ClassName "el-guidedog"

-- | A ClassName for el-hand-down
elHandDown :: ClassName
elHandDown = ClassName "el-hand-down"

-- | A ClassName for el-hand-left
elHandLeft :: ClassName
elHandLeft = ClassName "el-hand-left"

-- | A ClassName for el-hand-right
elHandRight :: ClassName
elHandRight = ClassName "el-hand-right"

-- | A ClassName for el-hand-up
elHandUp :: ClassName
elHandUp = ClassName "el-hand-up"

-- | A ClassName for el-hdd
elHdd :: ClassName
elHdd = ClassName "el-hdd"

-- | A ClassName for el-headphones
elHeadphones :: ClassName
elHeadphones = ClassName "el-headphones"

-- | A ClassName for el-hearing-impaired
elHearingImpaired :: ClassName
elHearingImpaired = ClassName "el-hearing-impaired"

-- | A ClassName for el-heart
elHeart :: ClassName
elHeart = ClassName "el-heart"

-- | A ClassName for el-heart-alt
elHeartAlt :: ClassName
elHeartAlt = ClassName "el-heart-alt"

-- | A ClassName for el-heart-empty
elHeartEmpty :: ClassName
elHeartEmpty = ClassName "el-heart-empty"

-- | A ClassName for el-home
elHome :: ClassName
elHome = ClassName "el-home"

-- | A ClassName for el-home-alt
elHomeAlt :: ClassName
elHomeAlt = ClassName "el-home-alt"

-- | A ClassName for el-hourglass
elHourglass :: ClassName
elHourglass = ClassName "el-hourglass"

-- | A ClassName for el-idea
elIdea :: ClassName
elIdea = ClassName "el-idea"

-- | A ClassName for el-idea-alt
elIdeaAlt :: ClassName
elIdeaAlt = ClassName "el-idea-alt"

-- | A ClassName for el-inbox
elInbox :: ClassName
elInbox = ClassName "el-inbox"

-- | A ClassName for el-inbox-alt
elInboxAlt :: ClassName
elInboxAlt = ClassName "el-inbox-alt"

-- | A ClassName for el-inbox-box
elInboxBox :: ClassName
elInboxBox = ClassName "el-inbox-box"

-- | A ClassName for el-indent-left
elIndentLeft :: ClassName
elIndentLeft = ClassName "el-indent-left"

-- | A ClassName for el-indent-right
elIndentRight :: ClassName
elIndentRight = ClassName "el-indent-right"

-- | A ClassName for el-info-circle
elInfoCircle :: ClassName
elInfoCircle = ClassName "el-info-circle"

-- | A ClassName for el-instagram
elInstagram :: ClassName
elInstagram = ClassName "el-instagram"

-- | A ClassName for el-inverse
elInverse :: ClassName
elInverse = ClassName "el-inverse"

-- | A ClassName for el-iphone-home
elIphoneHome :: ClassName
elIphoneHome = ClassName "el-iphone-home"

-- | A ClassName for el-italic
elItalic :: ClassName
elItalic = ClassName "el-italic"

-- | A ClassName for el-key
elKey :: ClassName
elKey = ClassName "el-key"

-- | A ClassName for el-laptop
elLaptop :: ClassName
elLaptop = ClassName "el-laptop"

-- | A ClassName for el-laptop-alt
elLaptopAlt :: ClassName
elLaptopAlt = ClassName "el-laptop-alt"

-- | A ClassName for el-lastfm
elLastfm :: ClassName
elLastfm = ClassName "el-lastfm"

-- | A ClassName for el-leaf
elLeaf :: ClassName
elLeaf = ClassName "el-leaf"

-- | A ClassName for el-lg
elLg :: ClassName
elLg = ClassName "el-lg"

-- | A ClassName for el-li
elLi :: ClassName
elLi = ClassName "el-li"

-- | A ClassName for el-lines
elLines :: ClassName
elLines = ClassName "el-lines"

-- | A ClassName for el-link
elLink :: ClassName
elLink = ClassName "el-link"

-- | A ClassName for el-linkedin
elLinkedin :: ClassName
elLinkedin = ClassName "el-linkedin"

-- | A ClassName for el-list
elList :: ClassName
elList = ClassName "el-list"

-- | A ClassName for el-list-alt
elListAlt :: ClassName
elListAlt = ClassName "el-list-alt"

-- | A ClassName for el-livejournal
elLivejournal :: ClassName
elLivejournal = ClassName "el-livejournal"

-- | A ClassName for el-lock
elLock :: ClassName
elLock = ClassName "el-lock"

-- | A ClassName for el-lock-alt
elLockAlt :: ClassName
elLockAlt = ClassName "el-lock-alt"

-- | A ClassName for el-magic
elMagic :: ClassName
elMagic = ClassName "el-magic"

-- | A ClassName for el-magnet
elMagnet :: ClassName
elMagnet = ClassName "el-magnet"

-- | A ClassName for el-male
elMale :: ClassName
elMale = ClassName "el-male"

-- | A ClassName for el-map-marker
elMapMarker :: ClassName
elMapMarker = ClassName "el-map-marker"

-- | A ClassName for el-map-marker-alt
elMapMarkerAlt :: ClassName
elMapMarkerAlt = ClassName "el-map-marker-alt"

-- | A ClassName for el-mic
elMic :: ClassName
elMic = ClassName "el-mic"

-- | A ClassName for el-mic-alt
elMicAlt :: ClassName
elMicAlt = ClassName "el-mic-alt"

-- | A ClassName for el-minus
elMinus :: ClassName
elMinus = ClassName "el-minus"

-- | A ClassName for el-minus-sign
elMinusSign :: ClassName
elMinusSign = ClassName "el-minus-sign"

-- | A ClassName for el-move
elMove :: ClassName
elMove = ClassName "el-move"

-- | A ClassName for el-music
elMusic :: ClassName
elMusic = ClassName "el-music"

-- | A ClassName for el-myspace
elMyspace :: ClassName
elMyspace = ClassName "el-myspace"

-- | A ClassName for el-network
elNetwork :: ClassName
elNetwork = ClassName "el-network"

-- | A ClassName for el-off
elOff :: ClassName
elOff = ClassName "el-off"

-- | A ClassName for el-ok
elOk :: ClassName
elOk = ClassName "el-ok"

-- | A ClassName for el-ok-circle
elOkCircle :: ClassName
elOkCircle = ClassName "el-ok-circle"

-- | A ClassName for el-ok-sign
elOkSign :: ClassName
elOkSign = ClassName "el-ok-sign"

-- | A ClassName for el-opensource
elOpensource :: ClassName
elOpensource = ClassName "el-opensource"

-- | A ClassName for el-paper-clip
elPaperClip :: ClassName
elPaperClip = ClassName "el-paper-clip"

-- | A ClassName for el-paper-clip-alt
elPaperClipAlt :: ClassName
elPaperClipAlt = ClassName "el-paper-clip-alt"

-- | A ClassName for el-path
elPath :: ClassName
elPath = ClassName "el-path"

-- | A ClassName for el-pause
elPause :: ClassName
elPause = ClassName "el-pause"

-- | A ClassName for el-pause-alt
elPauseAlt :: ClassName
elPauseAlt = ClassName "el-pause-alt"

-- | A ClassName for el-pencil
elPencil :: ClassName
elPencil = ClassName "el-pencil"

-- | A ClassName for el-pencil-alt
elPencilAlt :: ClassName
elPencilAlt = ClassName "el-pencil-alt"

-- | A ClassName for el-person
elPerson :: ClassName
elPerson = ClassName "el-person"

-- | A ClassName for el-phone
elPhone :: ClassName
elPhone = ClassName "el-phone"

-- | A ClassName for el-phone-alt
elPhoneAlt :: ClassName
elPhoneAlt = ClassName "el-phone-alt"

-- | A ClassName for el-photo
elPhoto :: ClassName
elPhoto = ClassName "el-photo"

-- | A ClassName for el-photo-alt
elPhotoAlt :: ClassName
elPhotoAlt = ClassName "el-photo-alt"

-- | A ClassName for el-picasa
elPicasa :: ClassName
elPicasa = ClassName "el-picasa"

-- | A ClassName for el-picture
elPicture :: ClassName
elPicture = ClassName "el-picture"

-- | A ClassName for el-pinterest
elPinterest :: ClassName
elPinterest = ClassName "el-pinterest"

-- | A ClassName for el-plane
elPlane :: ClassName
elPlane = ClassName "el-plane"

-- | A ClassName for el-play
elPlay :: ClassName
elPlay = ClassName "el-play"

-- | A ClassName for el-play-alt
elPlayAlt :: ClassName
elPlayAlt = ClassName "el-play-alt"

-- | A ClassName for el-play-circle
elPlayCircle :: ClassName
elPlayCircle = ClassName "el-play-circle"

-- | A ClassName for el-plurk
elPlurk :: ClassName
elPlurk = ClassName "el-plurk"

-- | A ClassName for el-plurk-alt
elPlurkAlt :: ClassName
elPlurkAlt = ClassName "el-plurk-alt"

-- | A ClassName for el-plus
elPlus :: ClassName
elPlus = ClassName "el-plus"

-- | A ClassName for el-plus-sign
elPlusSign :: ClassName
elPlusSign = ClassName "el-plus-sign"

-- | A ClassName for el-podcast
elPodcast :: ClassName
elPodcast = ClassName "el-podcast"

-- | A ClassName for el-print
elPrint :: ClassName
elPrint = ClassName "el-print"

-- | A ClassName for el-pulse
elPulse :: ClassName
elPulse = ClassName "el-pulse"

-- | A ClassName for el-puzzle
elPuzzle :: ClassName
elPuzzle = ClassName "el-puzzle"

-- | A ClassName for el-qrcode
elQrcode :: ClassName
elQrcode = ClassName "el-qrcode"

-- | A ClassName for el-question
elQuestion :: ClassName
elQuestion = ClassName "el-question"

-- | A ClassName for el-question-sign
elQuestionSign :: ClassName
elQuestionSign = ClassName "el-question-sign"

-- | A ClassName for el-quote-alt
elQuoteAlt :: ClassName
elQuoteAlt = ClassName "el-quote-alt"

-- | A ClassName for el-quote-right
elQuoteRight :: ClassName
elQuoteRight = ClassName "el-quote-right"

-- | A ClassName for el-quote-right-alt
elQuoteRightAlt :: ClassName
elQuoteRightAlt = ClassName "el-quote-right-alt"

-- | A ClassName for el-quotes
elQuotes :: ClassName
elQuotes = ClassName "el-quotes"

-- | A ClassName for el-random
elRandom :: ClassName
elRandom = ClassName "el-random"

-- | A ClassName for el-record
elRecord :: ClassName
elRecord = ClassName "el-record"

-- | A ClassName for el-reddit
elReddit :: ClassName
elReddit = ClassName "el-reddit"

-- | A ClassName for el-redux
elRedux :: ClassName
elRedux = ClassName "el-redux"

-- | A ClassName for el-refresh
elRefresh :: ClassName
elRefresh = ClassName "el-refresh"

-- | A ClassName for el-remove
elRemove :: ClassName
elRemove = ClassName "el-remove"

-- | A ClassName for el-remove-circle
elRemoveCircle :: ClassName
elRemoveCircle = ClassName "el-remove-circle"

-- | A ClassName for el-remove-sign
elRemoveSign :: ClassName
elRemoveSign = ClassName "el-remove-sign"

-- | A ClassName for el-repeat
elRepeat :: ClassName
elRepeat = ClassName "el-repeat"

-- | A ClassName for el-repeat-alt
elRepeatAlt :: ClassName
elRepeatAlt = ClassName "el-repeat-alt"

-- | A ClassName for el-resize-full
elResizeFull :: ClassName
elResizeFull = ClassName "el-resize-full"

-- | A ClassName for el-resize-horizontal
elResizeHorizontal :: ClassName
elResizeHorizontal = ClassName "el-resize-horizontal"

-- | A ClassName for el-resize-small
elResizeSmall :: ClassName
elResizeSmall = ClassName "el-resize-small"

-- | A ClassName for el-resize-vertical
elResizeVertical :: ClassName
elResizeVertical = ClassName "el-resize-vertical"

-- | A ClassName for el-return-key
elReturnKey :: ClassName
elReturnKey = ClassName "el-return-key"

-- | A ClassName for el-retweet
elRetweet :: ClassName
elRetweet = ClassName "el-retweet"

-- | A ClassName for el-reverse-alt
elReverseAlt :: ClassName
elReverseAlt = ClassName "el-reverse-alt"

-- | A ClassName for el-road
elRoad :: ClassName
elRoad = ClassName "el-road"

-- | A ClassName for el-rotate-180
elRotate180 :: ClassName
elRotate180 = ClassName "el-rotate-180"

-- | A ClassName for el-rotate-270
elRotate270 :: ClassName
elRotate270 = ClassName "el-rotate-270"

-- | A ClassName for el-rotate-90
elRotate90 :: ClassName
elRotate90 = ClassName "el-rotate-90"

-- | A ClassName for el-rss
elRss :: ClassName
elRss = ClassName "el-rss"

-- | A ClassName for el-scissors
elScissors :: ClassName
elScissors = ClassName "el-scissors"

-- | A ClassName for el-screen
elScreen :: ClassName
elScreen = ClassName "el-screen"

-- | A ClassName for el-screen-alt
elScreenAlt :: ClassName
elScreenAlt = ClassName "el-screen-alt"

-- | A ClassName for el-screenshot
elScreenshot :: ClassName
elScreenshot = ClassName "el-screenshot"

-- | A ClassName for el-search
elSearch :: ClassName
elSearch = ClassName "el-search"

-- | A ClassName for el-search-alt
elSearchAlt :: ClassName
elSearchAlt = ClassName "el-search-alt"

-- | A ClassName for el-share
elShare :: ClassName
elShare = ClassName "el-share"

-- | A ClassName for el-share-alt
elShareAlt :: ClassName
elShareAlt = ClassName "el-share-alt"

-- | A ClassName for el-shopping-cart
elShoppingCart :: ClassName
elShoppingCart = ClassName "el-shopping-cart"

-- | A ClassName for el-shopping-cart-sign
elShoppingCartSign :: ClassName
elShoppingCartSign = ClassName "el-shopping-cart-sign"

-- | A ClassName for el-signal
elSignal :: ClassName
elSignal = ClassName "el-signal"

-- | A ClassName for el-skype
elSkype :: ClassName
elSkype = ClassName "el-skype"

-- | A ClassName for el-slideshare
elSlideshare :: ClassName
elSlideshare = ClassName "el-slideshare"

-- | A ClassName for el-smiley
elSmiley :: ClassName
elSmiley = ClassName "el-smiley"

-- | A ClassName for el-smiley-alt
elSmileyAlt :: ClassName
elSmileyAlt = ClassName "el-smiley-alt"

-- | A ClassName for el-soundcloud
elSoundcloud :: ClassName
elSoundcloud = ClassName "el-soundcloud"

-- | A ClassName for el-speaker
elSpeaker :: ClassName
elSpeaker = ClassName "el-speaker"

-- | A ClassName for el-spin
elSpin :: ClassName
elSpin = ClassName "el-spin"

-- | A ClassName for el-spotify
elSpotify :: ClassName
elSpotify = ClassName "el-spotify"

-- | A ClassName for el-stack
elStack :: ClassName
elStack = ClassName "el-stack"

-- | A ClassName for el-stack-1x
elStack1X :: ClassName
elStack1X = ClassName "el-stack-1x"

-- | A ClassName for el-stack-2x
elStack2X :: ClassName
elStack2X = ClassName "el-stack-2x"

-- | A ClassName for el-stackoverflow
elStackoverflow :: ClassName
elStackoverflow = ClassName "el-stackoverflow"

-- | A ClassName for el-star
elStar :: ClassName
elStar = ClassName "el-star"

-- | A ClassName for el-star-alt
elStarAlt :: ClassName
elStarAlt = ClassName "el-star-alt"

-- | A ClassName for el-star-empty
elStarEmpty :: ClassName
elStarEmpty = ClassName "el-star-empty"

-- | A ClassName for el-step-backward
elStepBackward :: ClassName
elStepBackward = ClassName "el-step-backward"

-- | A ClassName for el-step-forward
elStepForward :: ClassName
elStepForward = ClassName "el-step-forward"

-- | A ClassName for el-stop
elStop :: ClassName
elStop = ClassName "el-stop"

-- | A ClassName for el-stop-alt
elStopAlt :: ClassName
elStopAlt = ClassName "el-stop-alt"

-- | A ClassName for el-stumbleupon
elStumbleupon :: ClassName
elStumbleupon = ClassName "el-stumbleupon"

-- | A ClassName for el-tag
elTag :: ClassName
elTag = ClassName "el-tag"

-- | A ClassName for el-tags
elTags :: ClassName
elTags = ClassName "el-tags"

-- | A ClassName for el-tasks
elTasks :: ClassName
elTasks = ClassName "el-tasks"

-- | A ClassName for el-text-height
elTextHeight :: ClassName
elTextHeight = ClassName "el-text-height"

-- | A ClassName for el-text-width
elTextWidth :: ClassName
elTextWidth = ClassName "el-text-width"

-- | A ClassName for el-th
elTh :: ClassName
elTh = ClassName "el-th"

-- | A ClassName for el-th-large
elThLarge :: ClassName
elThLarge = ClassName "el-th-large"

-- | A ClassName for el-th-list
elThList :: ClassName
elThList = ClassName "el-th-list"

-- | A ClassName for el-thumbs-down
elThumbsDown :: ClassName
elThumbsDown = ClassName "el-thumbs-down"

-- | A ClassName for el-thumbs-up
elThumbsUp :: ClassName
elThumbsUp = ClassName "el-thumbs-up"

-- | A ClassName for el-time
elTime :: ClassName
elTime = ClassName "el-time"

-- | A ClassName for el-time-alt
elTimeAlt :: ClassName
elTimeAlt = ClassName "el-time-alt"

-- | A ClassName for el-tint
elTint :: ClassName
elTint = ClassName "el-tint"

-- | A ClassName for el-torso
elTorso :: ClassName
elTorso = ClassName "el-torso"

-- | A ClassName for el-trash
elTrash :: ClassName
elTrash = ClassName "el-trash"

-- | A ClassName for el-trash-alt
elTrashAlt :: ClassName
elTrashAlt = ClassName "el-trash-alt"

-- | A ClassName for el-tumblr
elTumblr :: ClassName
elTumblr = ClassName "el-tumblr"

-- | A ClassName for el-twitter
elTwitter :: ClassName
elTwitter = ClassName "el-twitter"

-- | A ClassName for el-ul
elUl :: ClassName
elUl = ClassName "el-ul"

-- | A ClassName for el-universal-access
elUniversalAccess :: ClassName
elUniversalAccess = ClassName "el-universal-access"

-- | A ClassName for el-unlock
elUnlock :: ClassName
elUnlock = ClassName "el-unlock"

-- | A ClassName for el-unlock-alt
elUnlockAlt :: ClassName
elUnlockAlt = ClassName "el-unlock-alt"

-- | A ClassName for el-upload
elUpload :: ClassName
elUpload = ClassName "el-upload"

-- | A ClassName for el-usd
elUsd :: ClassName
elUsd = ClassName "el-usd"

-- | A ClassName for el-user
elUser :: ClassName
elUser = ClassName "el-user"

-- | A ClassName for el-viadeo
elViadeo :: ClassName
elViadeo = ClassName "el-viadeo"

-- | A ClassName for el-video
elVideo :: ClassName
elVideo = ClassName "el-video"

-- | A ClassName for el-video-alt
elVideoAlt :: ClassName
elVideoAlt = ClassName "el-video-alt"

-- | A ClassName for el-video-chat
elVideoChat :: ClassName
elVideoChat = ClassName "el-video-chat"

-- | A ClassName for el-view-mode
elViewMode :: ClassName
elViewMode = ClassName "el-view-mode"

-- | A ClassName for el-vimeo
elVimeo :: ClassName
elVimeo = ClassName "el-vimeo"

-- | A ClassName for el-vkontakte
elVkontakte :: ClassName
elVkontakte = ClassName "el-vkontakte"

-- | A ClassName for el-volume-down
elVolumeDown :: ClassName
elVolumeDown = ClassName "el-volume-down"

-- | A ClassName for el-volume-off
elVolumeOff :: ClassName
elVolumeOff = ClassName "el-volume-off"

-- | A ClassName for el-volume-up
elVolumeUp :: ClassName
elVolumeUp = ClassName "el-volume-up"

-- | A ClassName for el-w3c
elW3c :: ClassName
elW3c = ClassName "el-w3c"

-- | A ClassName for el-warning-sign
elWarningSign :: ClassName
elWarningSign = ClassName "el-warning-sign"

-- | A ClassName for el-website
elWebsite :: ClassName
elWebsite = ClassName "el-website"

-- | A ClassName for el-website-alt
elWebsiteAlt :: ClassName
elWebsiteAlt = ClassName "el-website-alt"

-- | A ClassName for el-wheelchair
elWheelchair :: ClassName
elWheelchair = ClassName "el-wheelchair"

-- | A ClassName for el-wordpress
elWordpress :: ClassName
elWordpress = ClassName "el-wordpress"

-- | A ClassName for el-wrench
elWrench :: ClassName
elWrench = ClassName "el-wrench"

-- | A ClassName for el-wrench-alt
elWrenchAlt :: ClassName
elWrenchAlt = ClassName "el-wrench-alt"

-- | A ClassName for el-youtube
elYoutube :: ClassName
elYoutube = ClassName "el-youtube"

-- | A ClassName for el-zoom-in
elZoomIn :: ClassName
elZoomIn = ClassName "el-zoom-in"

-- | A ClassName for el-zoom-out
elZoomOut :: ClassName
elZoomOut = ClassName "el-zoom-out"

